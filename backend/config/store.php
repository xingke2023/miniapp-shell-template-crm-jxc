<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 默认门店
    |--------------------------------------------------------------------------
    |
    | 暂不区分门店阶段使用：当无法从 token / 用户角色解析出门店时，
    | User::resolveStoreId() 回退到此门店 ID（而非返回 null 触发 403）。
    | 仍保留多门店能力——已绑门店的 token / 用户照常使用自己的门店。
    |
    */

    'default_id' => (int) env('DEFAULT_STORE_ID', 1),

];
